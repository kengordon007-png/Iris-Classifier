#Iris Classifier (Decision tree)
##Overview

## Quick start
--- bash
git clone https://github.com/kengordon007-png/iris-classifier.git
cd iris-classifier
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python src/train.py
